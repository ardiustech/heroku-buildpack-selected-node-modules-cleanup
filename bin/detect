#!/usr/bin/env bash
# bin/detect BUILD_DIR

BUILD_DIR=$1

NODE_MODULES_CLEANUP_DIR = $BUILD_DIR/devlops/modules/buildpacks/node_modules_cleanup/

if [ -f $NODE_MODULES_CLEANUP_DIR/buildpack-run.sh ]; then
    echo "Performing Node Modules Cleanup"

    exit 0
fi

exit 1
